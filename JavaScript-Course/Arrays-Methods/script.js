const evenNumbers = [0, 2, 4, 6]
const animals = ['Dog', 'Cat', 'Rat', 'Lion', 'Cow']

// EvenNumbers.shift()
// EvenNumbers.unshift() // This will add an element at the beginning

// const addedArray1 =  evenNumber.concat(animals)

// evenNumbers.indexof(2)

// const elementIndex =  animals.includes('Cat')

// Splice

// animals.splice(2,1)